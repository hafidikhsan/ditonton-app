import 'package:flutter_test/flutter_test.dart';
import 'package:series/presentation/bloc/now_playing_series_bloc.dart';

void main() {
  group('NowPlayingSeriesEvent', () {
    group('Load', () {
      test('Load Series', () {
        expect(
          LoadNowPlayingSeries(),
          LoadNowPlayingSeries(),
        );
      });
    });
  });
}
