name: about
description: A new Flutter package project.
version: 0.0.1
publish_to: none
homepage:

environment:
  sdk: ">=2.16.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  
  common:
    path: ../common
  ditonton:
    path: ../../a199-flutter-expert-project-main

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  mockito: ^5.0.8
  bloc_test: ^9.0.0

flutter:

  uses-material-design: true

  assets:
    - ../assets/
  
